function randomChance(num,leave){
    var rand = Math.floor(Math.random() * (100 - 1 + 1)) + 1;
    var prize = Math.floor(Math.random() * (50 - 1 + 1)) + 1;
    var quarters = num;

    
  return quarters; 
}


randomChance(10,20);